# Owner rulings & merge-grant timeline (as of 2026-07-11)

> **Status:** `reference`

Chat-only governance facts captured at close-out so nothing load-bearing is lost when the
project chat is archived. Verify against the session logs before relying on any single line.

- **Q-0265 — continuous mode (2026-07-10):** the lane runs continuous-mode (chain ticks +
  failsafe wake) rather than one-shot sessions.
- **~2026-07-10T21:00Z — owner strengthened continuous mode:** "always have something useful
  going on" (no idle; polish the roughest edge when the inbox is dry, never invent product
  intent).
- **2026-07-10T21:07Z — standing merge grant** (typed in session `cse_01CiurDYKFjjTjn9E56pWBBF`):
  owner grants explicit permission to merge/auto-merge any current or future PR without
  per-PR review — the purpose is review-AFTER-completion, when the whole product is finished.
- **2026-07-10T22:08Z — expanded grant** (typed in the coordinator session): "I explicitly
  grant you and all your agents full permissions to merge all PRs." Model: review after the
  product is finished; the substrate session-gate remains the in-flight quality gate (every
  PR lands only on green).

See also: `PLATFORM-LIMITS.md` (the seat-variance landing recipe), `control/status.md`
(`## Merge grant`), and `docs/retro/2026-07-11-self-review.md`.
