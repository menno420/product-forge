# Session — repo seed (dispatch copilot, superbot round-3 session)

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-10 ~18:3xZ (seeded from the superbot dispatch part-3
> session at the owner's ask; superbot PR #1955 is that session's card)

## What this session did

Born-right seed per fleet-manager `docs/gen2-blueprint.md` §1 + the founding package
(superbot `docs/planning/round3-founding-package-product-forge-2026-07-10.md`, role
framing owner-confirmed same day; rulings Q-0259 r.4 + Q-0264):

- substrate-kit v1.7.0 adopted (`bootstrap.py adopt`) **and engaged**: all interview
  slots answered with this repo's real design values, docs rendered live (0 unfilled),
  mode `guided`, `check --strict` green at commit.
- CI gate wired: `.github/workflows/substrate-gate.yml` (staged copy installed).
- Contract layer: root `README.md` (role · no-owning-lane test · pipeline position ·
  build ladder · money protocol · incubator/graduation) + `CONVENTIONS.md` (the
  blueprint day-0 landing rules, R21 grant written).
- `products/README.md` (one-product-one-subtree rule; empty at seed by design — the
  first routed ORDER creates the first subtree).
- `claims/` + `review-queue.md` + `PLATFORM-LIMITS.md` + `docs/retro/questions.md`
  (blueprint day-0 items; shared ground pre-resolved).

- **📊 Model:** fable-5 · high · docs-only (repo seed)

## 💡 Session idea

**Product-state honesty checker** — a tiny stdlib script that walks `products/*/README.md`
for the required state line (working/alpha/released) and fails if a subtree lacks a run
command or state claim. The "a product nobody can run is not shipped" rule is prose-only
at seed; its checker is what makes it real (the superbot #880→#882 lesson: a convention
without its checker drifts before the next session reads it).

## ⟲ Previous-session review

The idea-engine seed (same copilot, ~40 min earlier) went clean and this seed reused its
exact flow — the one improvement applied here: CONVENTIONS.md as a separate day-0 file
(the blueprint names it; the idea-engine seed folded conventions into the README, which
works but makes the merge-authority grant harder to cite by line).

## Handoff → the Product Forge coordinator's first boot

Seed (§2 boot step 3's seed-state train) is DONE by this session. Your first boot:
walking skeleton FIRST (branch → trivial PR → gate → merge, record which landing path
worked), then the owner clicks OWNER-ACTION (required-check name from the live gate
run), then arm the routine (§2 step 5) and heartbeat. Your first product ORDER arrives
in `control/inbox.md` via the manager — expected: the games-web experience (owner-named,
2026-07-10). Do not start it before the skeleton is proven.
